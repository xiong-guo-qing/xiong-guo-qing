# AI-PROJECT-RULES.md（项目版模板）

本文件用于补充当前项目的**专属上下文、边界和约定**。
未特别说明的通用规则，默认遵循全局 `AI-RULES.md`。

目标：
- 不重复全局通用规则
- 只记录项目特有信息
- 让 AI 在进入项目后快速理解上下文、边界、命令和风险点
- 让短期记忆与长期记忆的落点更稳定，便于后续检索

---

## 1. 项目概况

### 项目名称
- 在这里填写项目名

### 项目目标
- 这个项目是做什么的
- 核心用户是谁
- 解决什么问题

### 当前阶段
- 开发中 / 维护中 / 重构中 / 上线后优化 / 其他

### 成功标准
- 这个项目当前最重要的目标是什么
- 做改动时优先保证什么

---

## 2. 技术与运行环境

### 技术栈
- 前端：
- 后端：
- 数据库：
- 部署方式：
- 其他关键依赖：

### 运行与验证
- 安装依赖：
- 启动开发环境：
- 运行测试：
- 类型检查：
- lint：
- 构建：

### 环境说明
- 本地开发依赖：
- 特殊环境变量：
- 平台差异（Windows / macOS / Linux / WSL）：

---

## 3. 开始任务前必读内容

接到任务后，优先检查：
- `README.md`
- `AI-RULES.md`
- 当前 `AI-PROJECT-RULES.md`
- `overview.md`
- `architecture.md`
- `conventions.md`
- `commands.md`
- `gotchas.md`
- 与当前任务直接相关的代码和测试

如果 knowledge base 中存在相关记录，优先采用已有结论。

---

## 4. 项目目录与修改边界

### 重要目录
- `src/`：
- `app/`：
- `server/`：
- `docs/`：
- `tests/`：

### 修改边界
- 可以自由改动的区域：
- 需要谨慎改动的区域：
- 非明确要求不要动的区域：

### 敏感内容
- 生产配置位置：
- 数据库相关目录：
- 密钥 / 凭证相关文件：
- 不可逆操作注意事项：

---

## 5. 项目特有约定

请只填写**当前项目特有**的约定，不要重复全局规则。

例如：
- 命名规范
- 目录组织规范
- 组件拆分原则
- 接口封装方式
- 错误处理方式
- 日志规范
- 状态管理规范
- 样式规范
- 测试规范
- 提交或发布规范

如果项目已有固定模式，必须优先保持一致，不要引入新的风格。

---

## 6. 已知坑点与历史决策

在这里记录：
- 容易踩坑的依赖版本
- 历史 bug 根因
- 某些模块不能轻易修改的原因
- 特殊环境变量要求
- 特殊构建流程
- 部署注意事项
- 已拍板的技术决策
- 已明确否决过的方案

如果这里已有明确约束，不应反复提出被否掉的方案。

---

## 7. 推荐的短期记忆落点

建议在项目内使用如下结构：

```text
memory/
  inbox.md
  daily/
    YYYY-MM-DD.md
  tasks/
    <task-name>.md
  scratch/
    ideas.md
```

建议分工：
- `memory/inbox.md`：临时收集，还没归类的信息先放这里
- `memory/daily/YYYY-MM-DD.md`：当天上下文、进展、待续事项
- `memory/tasks/<task-name>.md`：任务目标、约束、过程、状态、待办
- `memory/scratch/`：草稿、一次性命令、临时方案比较

适合放入短期记忆的内容：
- 当前任务目标
- 临时约束
- 中间状态
- 排查过程
- 待验证判断
- 当前中断点和下一步建议

---

## 8. 推荐的长期知识落点

建议长期知识优先沉淀到稳定主题，而不是散落命名：

```text
ai-memory/
  projects/
    <project>/
      overview.md
      architecture.md
      conventions.md
      commands.md
      gotchas.md
  decisions/
  playbooks/
  stack/
  snippets/
```

适合更新到：
- `overview.md`：项目概况变化
- `architecture.md`：架构变化
- `conventions.md`：约定变化
- `commands.md`：新增常用命令
- `gotchas.md`：新坑与排障方案
- `decisions/`：关键决策
- `playbooks/`：稳定流程，如部署、排障、迁移步骤
- `basic-memory`：长期稳定、可复用知识

原则：
- 过程性内容先进入短期记忆
- 结论性内容再进入长期知识
- 项目知识、流程知识、技术栈知识尽量分层存放
- 长期文档尽量使用稳定命名，便于检索

---

## 9. 任务完成时建议汇报结构

任务结束时，尽量用下面结构汇报：
- 做了什么
- 改了哪些文件
- 为什么这样改
- 验证了什么
- 还存在哪些风险或建议

---

## 10. 可直接替换的精简版

如果你想写一个更短的项目版 `AI-PROJECT-RULES.md`，可以用这段：

> 本文件只补充当前项目的专属上下文、目录边界、运行命令、已知坑点和项目约定；未特别说明的通用行为默认遵循 `AI-RULES.md`。开始任务前先读取 README、AI-RULES.md、AI-PROJECT-RULES.md、相关文档和知识库，再结合当前任务定位需要修改的代码与测试。过程性信息优先写入 `memory/inbox.md`、`memory/daily/`、`memory/tasks/`、`memory/scratch/` 等短期记忆位置；稳定结论再写入 `overview.md`、`architecture.md`、`conventions.md`、`commands.md`、`gotchas.md`、`decisions/` 或长期知识库。
