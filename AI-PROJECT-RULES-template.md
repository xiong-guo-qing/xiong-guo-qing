# AI-PROJECT-RULES.md（项目版模板）

本文件定义 AI 编程助手在当前项目中的专属执行规则。
如与全局 `AI-RULES.md` 冲突，以**项目版**为准。

---

## 1. 项目概况

### 项目名称
- 在这里填写项目名

### 项目目标
- 这个项目是做什么的
- 核心用户是谁
- 当前阶段是什么（开发中 / 维护中 / 重构中 / 上线后优化等）

### 技术栈
- 前端：
- 后端：
- 数据库：
- 部署方式：
- 其他关键依赖：

---

## 2. 工作原则

在本项目中，AI 编程助手必须：
- 先读本文件，再读相关代码
- 先搜索项目知识库，再开始实现
- 优先复用现有实现方式
- 进行最小必要修改
- 修改后尽可能验证
- 发现新知识时，及时回写到知识库或项目文档

---

## 3. 开始任务前必须阅读的内容

接到任务后，优先检查：
- `README.md`
- `AI-RULES.md`
- `AI-PROJECT-RULES.md`
- `overview.md`
- `architecture.md`
- `conventions.md`
- `commands.md`
- `gotchas.md`
- 与当前任务直接相关的代码和测试

如果 knowledge base 中存在相关记录，优先采用已有结论。

---

## 4. 项目目录与修改边界

### 重要目录
- `src/`：
- `app/`：
- `server/`：
- `docs/`：
- `scripts/`：
- `tests/`：

### 修改边界
- 可以自由改动的区域：
- 需要谨慎改动的区域：
- 非明确要求不要动的区域：

---

## 5. 项目编码约定

请在这里填写本项目特有的约定，例如：
- 命名规范
- 目录组织规范
- 组件拆分原则
- 接口封装方式
- 错误处理方式
- 日志规范
- 状态管理规范
- 样式规范
- 测试规范

如果项目已有固定模式，必须优先保持一致，不要引入新的风格。

---

## 6. 常用命令

在这里填写本项目常用命令，例如：

```bash
# 安装依赖

# 启动开发环境

# 运行测试

# 类型检查

# lint

# 构建
```

如果命令存在平台差异，也在这里说明。

---

## 7. 已知坑点与约束

在这里记录：
- 容易踩坑的依赖版本
- 历史 bug 根因
- 某些模块不能轻易修改的原因
- 特殊环境变量要求
- 特殊构建流程
- 部署注意事项

如果这里已有明确约束，AI 编程助手不应反复提出被否掉的方案。

---

## 8. 功能开发要求

开发新功能时：
- 先确认输入输出
- 先确认是否影响现有行为
- 优先沿用现有模块和实现方式
- 非必要不要新增依赖
- 非必要不要新建大型抽象层
- 若影响用户可见行为，应同步更新文档和测试

---

## 9. Bug 修复要求

修 bug 时：
- 先理解问题现象
- 优先定位根因
- 做最小修复
- 尽量补回归验证
- 若这是高频问题，修复后写入 `gotchas.md` 或知识库

不要在未定位根因时用大面积补丁掩盖问题。

---

## 10. 文档 / knowledge base 回写要求

任务完成后，如果产生了稳定知识，应更新到合适位置：
- `overview.md`：项目概况变化
- `architecture.md`：架构变化
- `conventions.md`：约定变化
- `commands.md`：新增常用命令
- `gotchas.md`：新坑与排障方案
- `decisions/`：关键决策

如果只是临时猜测，不要写入长期文档。

---

## 11. 输出要求

任务结束时，尽量用下面结构汇报：
- 做了什么
- 改了哪些文件
- 为什么这样改
- 验证了什么
- 还存在哪些风险或建议

---

## 12. 可直接替换的精简版

如果你想写一个更短的项目版 `AI-PROJECT-RULES.md`，可以用这段：

> 在本项目中，先读取 README、AI-RULES.md、AI-PROJECT-RULES.md、相关文档和知识库，再开始实现。优先遵循现有架构和代码风格，先搜索是否已有类似实现。只做最小必要修改，不做无关重构。修改后尽可能运行测试、类型检查、lint 或构建。若产生稳定可复用的知识，写回项目文档或长期知识库；未验证内容不要写入长期知识。
