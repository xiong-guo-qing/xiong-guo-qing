# BASIC-MEMORY-RULES.md

本文件定义 AI 编程工具在使用 **basic-memory** 作为共享本地知识库时的工作规范。

目标：
- 让不同 AI 编程工具共享同一套长期记忆
- 与项目中的短期记忆层配合工作，而不是相互混淆
- 减少重复解释项目背景
- 避免知识污染、重复记录和低质量写入
- 让知识库既能被 AI 使用，也能被人类长期维护

---

## 1. 基本原则

1. **同一套知识库，多工具共享**
   - Claude Code、Cursor、Codex、OpenClaw、Antigravity 等工具应尽量读写同一套 basic-memory 数据源。
   - 不要为每个工具维护独立长期记忆。

2. **basic-memory 是长期记忆，不是短期会话记录**
   - 对项目长期有价值的信息，应进入知识库。
   - 当前任务过程、临时约束、中间分析应保留在短期记忆层，而不是直接写入长期知识库。

3. **人类可读、可维护优先**
   - 知识组织应尽量清晰，方便 AI 读取，也方便人类直接编辑。

4. **宁可少写，也不要写脏**
   - 未验证的信息不要写入长期知识库。
   - 一次性临时内容不要污染长期知识。

---

## 2. AI 在开始任务前必须做什么

在处理项目任务前，AI 应优先：

1. 搜索 basic-memory 中与当前项目相关的内容
2. 读取项目核心知识文档，例如：
   - `overview.md`
   - `architecture.md`
   - `conventions.md`
   - `commands.md`
   - `gotchas.md`
3. 检查是否已有类似问题、类似实现或历史决策
4. 如果知识库已有明确结论，优先遵守，不重复发明方案
5. 再结合当前会话或项目内短期记忆目录，理解当前任务状态与临时约束

---

## 3. 哪些信息应该写入知识库

适合写入长期知识库的内容：

### 项目背景
- 项目目标
- 核心模块说明
- 技术栈
- 服务关系
- 数据流概览

### 稳定约定
- 命名规范
- 目录约定
- 提交规范
- 错误处理约定
- 测试约定

### 重要命令
- 启动命令
- 构建命令
- 测试命令
- 部署命令
- 调试命令

### 架构与决策
- 关键设计方案
- 已拍板的技术决策
- 不应轻易改动的部分
- 方案取舍原因

### 排障经验
- 常见错误
- 根因分析
- 已验证修复方案
- 环境坑 / 依赖坑 / 部署坑

### 可复用实现知识
- 某类功能的推荐实现方式
- 某个模块的调用约束
- 哪些工具函数 / 组件 / 服务应该优先复用

---

## 4. 哪些信息不应该写入长期知识库

以下内容不要直接进入长期知识库：

- 未验证的猜测
- 临时任务过程记录
- 杂乱无结论的调试日志
- 一次性沟通内容
- 纯粹基于个人偏好的建议
- 很快会过期的信息

如果内容可能有用但尚未确认，应先放入短期记忆层，例如：
- `memory/inbox.md`
- `memory/tasks/`
- `memory/daily/`
- `scratch/`
- 临时笔记区

等确认后再整理进正式知识文档。

---

## 5. 推荐的知识库结构

```text
ai-memory/
  inbox/
  projects/
    project-a/
      overview.md
      architecture.md
      conventions.md
      commands.md
      gotchas.md
      roadmap.md
  decisions/
  stack/
  playbooks/
  snippets/
```

说明：

- `projects/`：按项目组织核心知识
- `decisions/`：记录 ADR / 关键决策
- `stack/`：记录技术栈通用知识（如 React、Docker、Postgres）
- `playbooks/`：记录流程型知识（部署、排查、上线、回滚）
- `snippets/`：记录高频命令、模板、片段
- `inbox/`：临时收集，待整理

### 5.1 推荐配套的短期记忆结构

如果项目需要本地短期记忆目录，推荐使用：

```text
memory/
  inbox.md
  daily/
  tasks/
```

建议分工：
- `memory/inbox.md`：临时收集、待整理信息
- `memory/daily/YYYY-MM-DD.md`：按天记录上下文
- `memory/tasks/<task-name>.md`：按任务记录过程与中间结论

原则：
- 短期信息先进 `memory/`
- 稳定结论再进入 `basic-memory`
- 不要把同一类内容分散到多个无规律目录中

---

## 6. 写入规则

### 先搜再写
写入前，先搜索已有内容，避免：
- 重复记录
- 冲突记录
- 多个文件表达同一件事

### 优先追加和整理
对于已有文档：
- 优先补充
- 优先修正
- 优先归类整理

不要每次都新建一堆碎片文档。

### 一次只写已确认内容
写入时，只记录：
- 已验证的事实
- 已确认的结论
- 可复用的经验

不要把模糊推断写成既定事实。

### 保持简洁、结构化
写入知识时尽量：
- 用简短标题
- 用列表或分段
- 用明确结论表达
- 避免堆大段无重点叙述

### 先短期，后长期
如果信息仍处于排查、讨论、实验阶段：
- 先记录到短期记忆层
- 等结论稳定后，再提炼写入 basic-memory

---

## 7. 更新规则

当下列内容发生变化时，应同步更新知识库：

- 项目结构变化
- 启动方式变化
- 配置项变化
- 环境变量变化
- 构建 / 部署流程变化
- 架构调整
- 新发现的高频问题
- 已确认的技术决策变化

如果旧知识已经失效，应：
- 更新原文档
- 标记废弃结论
- 避免新旧结论冲突并存

---

## 8. 多 AI 工具协作规则

多个 AI 工具共用 knowledge base 时，必须遵守：

1. 不覆盖别人已经沉淀的稳定知识
2. 不用不同命名重复记录同一主题
3. 不把聊天式废话写入知识库
4. 不把临时推理链直接保存为长期知识
5. 如果发现知识冲突，优先标注问题并整理，而不是继续叠加混乱内容
6. 如果只是当前任务临时例外，优先记录到短期记忆层，不要污染长期知识库

---

## 9. 推荐给 AI 的执行提示

如果某个 AI 工具支持系统提示词，可加入以下要求：

> 在开始处理任务前，先搜索并读取 basic-memory 中与项目相关的知识。优先参考 overview、architecture、conventions、commands 和 gotchas 等核心文档。再结合当前会话与项目内短期记忆目录理解任务状态。若任务中产生了已验证、可复用的知识，写回长期知识库；若只是临时猜测、过程记录或未确认分析，先放入短期记忆，不要写入长期知识库。保持知识结构清晰、简洁、可维护，避免重复和冲突。

---

## 10. 最终目标

basic-memory 不是聊天记录堆积区，而是：

- AI 的长期项目记忆
- 人类可维护的本地知识库
- 多工具共享上下文的统一长期层

判断一条内容该不该写进去，只问一个问题：

**“这条信息在下周、下个月、换一个 AI 工具时，是否仍然有价值？”**

如果答案是“有”，再写进去。
