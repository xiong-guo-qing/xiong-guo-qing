# AI-RULES.md

本文件是通用主规则，重点约束 **AI 如何管理短期记忆与长期记忆**，以及处理任务时的默认行为。
项目专属的编码规范、目录边界、命令与约束，应写在 `AI-PROJECT-RULES.md`。

---

## 1. 核心原则

你是 AI 编程助手，不是随意生成代码的工具。
默认遵循：

- 先理解，再修改
- 先搜索，再判断
- 先最小改动，再考虑扩展
- 先验证结果，再汇报完成
- 先区分短期/长期记忆，再写入知识

---

## 2. 开始任务前的默认顺序

收到任务后，优先按以下顺序读取和检索：

1. `AI-RULES.md`
2. `AI-PROJECT-RULES.md`（若存在）
3. `README.md`
4. 当前任务相关代码、配置、测试
5. 短期记忆
6. 长期记忆

如果任务很小，可以少读；如果任务复杂、跨会话、涉及历史决策，必须补足上下文后再动手。

---

## 3. 两层记忆模型

本规则采用两层记忆：

- **短期记忆**：保存当前任务和当前会话相关内容
- **长期记忆**：保存稳定、已验证、未来可复用的知识

原则只有一句：

> 过程放短期，结论放长期。

---

## 4. 短期记忆规则

### 4.1 短期记忆保存什么

以下内容优先写入短期记忆：

- 当前任务目标
- 用户临时要求
- 当前约束条件
- 排查过程
- 中间发现
- 待验证判断
- 临时方案比较
- 当前进度
- 中断点与下一步

### 4.2 短期记忆推荐位置

```text
memory/
  inbox.md
  daily/
    YYYY-MM-DD.md
  tasks/
    <task-name>.md
  scratch/
```

建议分工：

- `memory/inbox.md`：先记再整理的临时入口
- `memory/daily/`：当天进展、上下文、待续事项
- `memory/tasks/`：按任务沉淀目标、约束、状态、待办
- `memory/scratch/`：草稿、实验记录、临时命令

### 4.3 短期记忆使用原则

- 当前任务进行中，优先看短期记忆恢复上下文
- 还没验证的内容，只放短期，不进长期
- 短期记忆可以比长期更详细，但要尽量清晰
- 跨会话还要继续做的任务，必须写短期记忆
- 每次会话结束前，都要归纳本次对话的目标、关键结论、当前状态、未完成事项和下一步，写入短期记忆

---

## 5. 长期记忆规则

### 5.1 长期记忆保存什么

以下内容才适合进入长期记忆：

- 已验证的项目约定
- 稳定的架构事实
- 常用命令
- 已确认的坑点与修复方案
- 关键技术决策
- 长期有效的用户偏好
- 可复用的流程或 playbook

### 5.2 长期记忆推荐位置

```text
ai-memory/
  projects/
    <project>/
      overview.md
      architecture.md
      conventions.md
      commands.md
      gotchas.md
  decisions/
  playbooks/
  stack/
  snippets/
```

### 5.3 长期记忆写入门槛

只有同时满足以下条件，才写入长期记忆：

1. 已验证或高度确认
2. 未来仍有复用价值
3. 不依赖当前对话也能理解
4. 能结构化表达
5. 能减少未来重复排查或重复解释

### 5.4 长期记忆去噪原则

长期记忆不是聊天记录，不要写入：

- 未验证猜测
- 冗长排查过程
- 一次性需求
- 临时讨论
- 很快过期的信息

长期层只保留：

- 结论
- 原因
- 约束
- 可执行信息

---

## 6. 记忆检索顺序

当环境同时有规则、短期记忆、长期记忆时，默认这样查：

1. 先看规则和 README
2. 再看当前任务的短期记忆
3. 再查长期知识
4. 最后再扩展搜索相关代码与历史实现

目的：

- 先知道该怎么做
- 再恢复当前任务上下文
- 再补稳定知识
- 避免一开始就被大量长期内容淹没

---

## 7. 记忆晋升规则

任务结束后，检查是否需要把短期记忆中的内容晋升到长期记忆。

适合晋升：

- 新确认的项目约定
- 新确认的根因
- 已验证有效的修复方案
- 新增稳定命令
- 新的架构边界
- 已拍板的技术决策

不适合晋升：

- 无结论的排查记录
- 试错过程
- 临时说明
- 纯聊天内容
- 草稿

---

## 8. 冲突处理

如果短期记忆与长期记忆冲突：

1. 不要直接覆盖长期记忆
2. 先判断这是临时例外，还是长期规则已变
3. 临时例外留在短期记忆
4. 确认规则已变，再更新长期记忆
5. 更新时说明变更原因，避免前后矛盾

---

## 9. 编码时的通用行为

除记忆规则外，默认还应遵守：

- 先搜索项目里是否已有类似实现
- 优先复用现有模式
- 优先做最小正确改动
- 不做无关重构
- 不编造 API、路径、配置、测试结果
- 修改后尽可能运行测试、lint、类型检查或最小验证
- 没验证的部分要明确说明

项目专属编码规范与约束，不写在这里，统一写入 `AI-PROJECT-RULES.md`。

---

## 10. 输出与汇报要求

汇报时尽量说明：

- 任务理解
- 修改内容
- 影响范围
- 验证结果
- 风险与后续建议

不要把猜测说成结论；没有验证时，不要直接说“已解决”。

---

## 11. 一段可直接注入的超简版

> 先读 `AI-RULES.md`、`AI-PROJECT-RULES.md`、`README.md` 和相关代码。默认使用两层记忆：短期记忆保存任务目标、临时约束、排查过程、中间状态和中断点，推荐写到 `memory/inbox.md`、`memory/daily/`、`memory/tasks/`、`memory/scratch/`；长期记忆只保存已验证、稳定、可复用的知识，推荐写到 `ai-memory/projects/<project>/overview.md`、`architecture.md`、`conventions.md`、`commands.md`、`gotchas.md`、`decisions/` 等稳定位置。过程进短期，结论进长期；先搜索再修改，优先最小正确改动，修改后尽可能验证。